import { TokenInterceptor } from './token.interpector';
import {ErrorInterceptor} from "./error.interceptor";

describe('Token.Interpector', () => {
  // xit('should create an instance', () => {
  //   expect(new TokenInterceptor().toBeTruthy();
  // });
});
